
#### Tabela de contingência: Cidade e Q18

```{r}
contingency(df, "Cidade", "Q18", "Cidade", caption="Tabela de contingência: Cidade e Q18.", base = output)
```

#### Gráfico de barras: Cidade e Q18

Aparentemente as duas variáveis _Cidade_ e _Q18_ não estão associadas.

```{r}
grafico_2(df, "Cidade", "Q18", 'Cidade', base = fig_path)
```

#### Teste qui-quadrado

Como o valor-p é maior que $\alpha=0,05$, não rejeitamos $H_0$ e não temos evidência evidência estatística que as duas variáveis estão associadas. 

```{r}
df |> 
  qui_test("Cidade", "Q18", base = output)
```

\cleardoublepage

#### Medidas de Resumo Q18 por Cidade


```{r}
dados |> 
  resume_measures_2("Cidade", "Q18", base = output)
```

#### Boxplot de Q18 por Cidade 


```{r}
dados |>
  boxplot_group('Q18', 'Cidade', 'Q18', 'Cidade', base = fig_path)
```

#### Teste de Kruskal-Wallis de Q18 por Cidade


Como o valor-p é menor que $\alpha=0,05$, rejeitamos $H_0$ e as medianas de $Q18$ entre as crianças de diversas cidades não são todas iguais.

```{r}
dados |> 
  kruskal_wallis_test('Q18', 'Cidade', 'Valores-p para comparação múltipla de medianas: Q18 e Cidade.', base = output)
```

#### Teste de Nemeyi de Q18 por Cidade

Os valores-p são maiores que $\alpha=0,05$, então não rejeitamos $H_0$, ou seja, as medianas são iguais para todos os pares de cidades.

```{r}
dados |> 
  nemenyi_tests('Q18', 'Cidade', caption = 'Teste de Nemeyi de Q18 por Cidade.', base = output)
``` 