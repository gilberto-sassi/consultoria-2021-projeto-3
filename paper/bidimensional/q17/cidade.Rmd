
#### Tabela de contingência: Cidade e Q17

```{r}
contingency(df, "Cidade", "Q17", "Cidade", caption="Tabela de contingência: Cidade e Q17.", base = output)
```

#### Gráfico de barras: Cidade e Q17

Aparentemente as duas variáveis _Cidade_ e _Q17_ não estão associadas.

```{r}
grafico_2(df, "Cidade", "Q17", 'Cidade', base = fig_path)
```

#### Teste qui-quadrado

Como o valor-p é maior que $\alpha=0,05$, não rejeitamos $H_0$ e não temos evidência evidência estatística que as duas variáveis estão associadas. 

```{r}
df |> 
  qui_test("Cidade", "Q17", base = output)
```

\cleardoublepage

#### Medidas de Resumo Q17 por Cidade


```{r}
dados |> 
  resume_measures_2("Cidade", "Q17", base = output)
```

#### Boxplot de Q17 por Cidade 


```{r}
dados |>
  boxplot_group('Q17', 'Cidade', 'Q17', 'Cidade', base = fig_path)
```

#### Teste de Kruskal-Wallis de Q17 por Cidade


Como o valor-p é maior que $\alpha=0,05$, não rejeitamos $H_0$ e as medianas de $Q17$ entre as crianças de diversas cidades são iguais.

```{r}
dados |> 
  kruskal_wallis_test('Q17', 'Cidade', 'Valores-p para comparação múltipla de medianas: Q17 e Cidade.', base = output)
```

#### Teste de Nemeyi de Q17 por Cidade

Os valores-p são maiores que $\alpha=0,05$, então não rejeitamos $H_0$, ou seja, as medianas são iguais para todos os pares de cidades.

```{r}
dados |> 
  nemenyi_tests('Q17', 'Cidade', caption = 'Teste de Nemeyi de Q17 por Cidade.', base = output)
``` 