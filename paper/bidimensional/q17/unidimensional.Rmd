#### Gráfico de barras: Q17

```{r}
grafico(df, "Q17", "Q17", base = fig_path)
```

#### Tabela de distribuição: Q17

```{r}
tabela(df, 'Q17', 'Q17', 'Que falte comida nos supermercados', base = output)
```


#### Medidas de resumo: Q17

```{r}
resume_measures(dados, "Q17", base = output)
```
