
#### Tabela de contingência: Cidade e Q13

```{r}
contingency(df, "Cidade", "Q13", "Cidade", caption="Tabela de contingência: Cidade e Q13.", base = output)
```

#### Gráfico de barras: Cidade e Q13

Aparentemente as duas variáveis _Cidade_ e _Q13_ não estão associadas.

```{r}
grafico_2(df, "Cidade", "Q13", 'Cidade', base = fig_path)
```

#### Teste qui-quadrado

Como o valor-p é maior que $\alpha=0,05$, não rejeitamos $H_0$ e não temos evidência evidência estatística que as duas variáveis estão associadas. 

```{r}
df |> 
  qui_test("Cidade", "Q13", base = output)
```

\cleardoublepage

#### Medidas de Resumo Q13 por Cidade


```{r}
dados |> 
  resume_measures_2("Cidade", "Q13", base = output)
```

#### Boxplot de Q13 por Cidade 


```{r}
dados |>
  boxplot_group('Q13', 'Cidade', 'Q13', 'Cidade', base = fig_path)
```

#### Teste de Kruskal-Wallis de Q13 por Cidade


Como o valor-p é maior que $\alpha=0,05$, não rejeitamos $H_0$ e as medianas de $Q13$ entre as crianças de diversas cidades são iguais.

```{r}
dados |> 
  kruskal_wallis_test('Q13', 'Cidade', 'Valores-p para comparação múltipla de medianas: Q13 e Cidade.', base = output)
```

#### Teste de Nemeyi de Q13 por Cidade

Como os valores-p são maiores que $\alpha=0,05$, não rejeitamos $H_0$ e as medianas de $Q12$ entre as crianças de diversas cidades são iguais.

```{r}
dados_raca |> 
  nemenyi_tests('Q13', 'raca', caption = 'Teste de Nemeyi de Q13 por Cidade.', base = output)
``` 