#### Gráfico de barras: Q22

```{r}
grafico(df, "Q22", "Q22", base = fig_path)
```

#### Tabela de distribuição: Q22

```{r}
tabela(df, 'Q22', 'Q22', 'Que eu não possa brincar com meus amigos como eu brincava antes', base = output)
```


#### Medidas de resumo: Q22

```{r}
resume_measures(dados, "Q22", base = output)
```
