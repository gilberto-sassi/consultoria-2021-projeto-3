
#### Tabela de contingência: Cidade e Q22

```{r}
contingency(df, "Cidade", "Q22", "Cidade", caption="Tabela de contingência: Cidade e Q22.", base = output)
```

#### Gráfico de barras: Cidade e Q22

```{r}
grafico_2(df, "Cidade", "Q22", 'Cidade', base = fig_path)
```

#### Teste qui-quadrado

Como o valor-p é maior que $\alpha=0,05$, não rejeitamos $H_0$ e não temos evidência evidência estatística que as duas variáveis estão associadas. 

```{r}
df |> 
  qui_test("Cidade", "Q22", base = output)
```

\cleardoublepage

#### Medidas de Resumo Q22 por Cidade


```{r}
dados |> 
  resume_measures_2("Cidade", "Q22", base = output)
```

#### Boxplot de Q22 por Cidade 


```{r}
dados |>
  boxplot_group('Q22', 'Cidade', 'Q22', 'Cidade', base = fig_path)
```

#### Teste de Kruskal-Wallis de Q22 por Cidade


Como o valor-p é maior que $\alpha=0,05$, não rejeitamos $H_0$ e as medianas de $Q22$ entre as crianças de diversas cidades são todas iguais.

```{r}
dados |> 
  kruskal_wallis_test('Q22', 'Cidade', 'Valores-p para comparação múltipla de medianas: Q22 e Cidade.', base = output)
```

#### Teste de Nemeyi de Q22 por Cidade

Os valores-p são maiores que $\alpha=0,05$, então não rejeitamos $H_0$, ou seja, as medianas são iguais para todos os pares de cidades.

```{r}
dados |> 
  nemenyi_tests('Q22', 'Cidade', caption = 'Teste de Nemeyi de Q22 por Cidade.', base = output)
``` 