#### Tabela de contingência: Idade e Q22

```{r}
contingency(df, "Idade", "Q22", "Idade", caption="Tabela de contingência: Idade e Q22.", base = output)
```

#### Gráfico de barras: Idade e Q22

```{r}
grafico_2(df, "Idade", "Q22", 'Idade', base = fig_path)
```

#### Teste qui-quadrado

Como o valor-p está menor que $\alpha=0,05$, rejeitamos $H_0$ e temos temos evidência evidência estatística que as duas variáveis estão associadas. 

```{r}
df |> 
  qui_test("Idade", "Q22", base = output)
```

\cleardoublepage

#### Medidas de Resumo Q22 por Idade

```{r}
dados |> 
  resume_measures_2("Idade", "Q22", base = output)
```

#### Boxplot de Q22 por Idade 

```{r}
dados |>
  boxplot_group('Q22', 'Idade', 'Q22', 'Idade', base = fig_path)
```

#### Teste de Kruskal-Wallis de Q22 por Idade

Como o valor-p é menor que $\alpha=0,05$, rejeitamos $H_0$ e as medianas de $Q22$ dentro de cada categoria de Idade são diferentes.

```{r}
df |> 
  kruskal_wallis_test('Q22', 'Idade', 'Valores-p para comparação múltipla de medianas: Q22 e Idade.', base = output)
```

#### Teste de Nemeyi de Q22 por Idade

Os valores-p são todos maiores que $\alpha=0,05$ e não rejeitamos $H_0$ para todos os pares de idade, ou seja, as medianas são iguais para cada par de idade.

```{r}
dados |> 
  nemenyi_tests('Q22', 'Idade', caption = 'Teste de Nemeyi de Q22 por Idade.', base = output)
```
